{
  "name": "rem-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "pnpm --filter rem-agent-core build && pnpm --filter rem-agent-service build && pnpm --filter rem-agent-client build && pnpm --filter rem-agent-web build",
    "test": "vitest run packages/core/tests && pnpm --filter rem-agent-service test && pnpm --filter rem-agent-client test && pnpm --filter rem-agent-web test",
    "test:watch": "vitest packages/core/tests",
    "typecheck": "pnpm --filter rem-agent-core build && pnpm --filter rem-agent-core typecheck && pnpm --filter rem-agent-service typecheck && pnpm --filter rem-agent-client typecheck && pnpm --filter rem-agent-web typecheck",
    "dev:web": "pnpm --filter rem-agent-web dev",
    "check:structure": "pnpm --filter rem-agent-core check-structure"
  },
  "devDependencies": {
    "@opentui/core-darwin-arm64": "^0.4.2",
    "@testing-library/jest-dom": "^6.4.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^1.6.0",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
