{
  "name": "rem-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm --filter rem-agent-core build && pnpm --filter rem-agent-core typecheck && pnpm --filter rem-agent-bridge build && pnpm --filter rem-agent-bridge typecheck && pnpm --filter rem-agent-server build && pnpm --filter rem-agent-server typecheck && pnpm --filter rem-agent-tui build && pnpm --filter rem-agent-tui typecheck && pnpm --filter rem-agent-demo typecheck",
    "demo": "pnpm --filter rem-agent-demo start",
    "demo:server": "pnpm --filter rem-agent-demo start -- server",
    "demo:tui": "pnpm --filter rem-agent-demo start -- tui",
    "demo:debug": "REM_AGENT_DEBUG=1 pnpm --filter rem-agent-demo start"
  },
  "devDependencies": {
    "@opentui/core-darwin-arm64": "^0.4.2",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
